(executable
 ((name reynir)
  (package websocket-lwt)
  (modules reynir)
  (libraries (websocket-lwt))))

(executable
 ((name upgrade_connection)
  (package websocket-lwt)
  (modules upgrade_connection)
  (libraries (websocket-lwt.cohttp))))

(executable
 ((name bench_server)
  (package websocket-lwt)
  (modules bench_server)
  (libraries (websocket-lwt))))
